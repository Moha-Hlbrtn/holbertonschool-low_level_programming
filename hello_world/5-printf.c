#include <stdio.h>

/**
 * main - Point d'entrée
 * Description: Affiche un message avec printf
 * Return: Always 0 (Success)
 */
int main(void)
{
    printf("with proper grammar, but the outcome is a piece of art\n");
    return (0);
}
