#include "main.h"

/**
 * strlen - returns the length of a string
 * @s: the parameter to be returned
 * return: 0 Success
 */
int _strlen(char *s)
{
	int len;
	
	for (len = 0; s[len] != '\0'; len++)
	{
	}
	return len;
}
